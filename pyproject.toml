[build-system]
requires = ["setuptools>=75", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "prxmpt-coach"
version = "1.0.8"
description = "A consent-based Windows interview practice and meeting coaching assistant."
readme = "README.md"
requires-python = ">=3.12,<3.14"
authors = [{ name = "Hamza Salahuddin" }]
dependencies = [
  "PySide6>=6.8,<7",
  "numpy>=2.0,<3",
  "SoundCard>=0.4.6,<0.5",
  "sounddevice>=0.5.1,<0.6",
  "faster-whisper>=1.2,<2",
  "huggingface-hub>=0.30,<2",
  "webrtcvad-wheels>=2.0.14,<3",
  "openai>=2.0,<3",
  "pypdf>=5,<7",
  "python-docx>=1.1,<2",
  "keyring>=25,<27",
  "pynput>=1.7,<2",
  "packaging>=24,<27",
]

[project.optional-dependencies]
dev = [
  "pytest>=8.3,<10",
  "pytest-cov>=6,<8",
  "pyinstaller>=6.14,<7",
]

[project.scripts]
prxmpt = "clearcue.main:main"
clearcue = "clearcue.main:main"

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.package-data]
clearcue = ["assets/*.png", "assets/*.ico", "assets/*.svg", "assets/*.json"]

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
